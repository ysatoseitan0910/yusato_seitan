# 推しぷろふぃーる（card.html）作業メモ

最終更新：2026-07-15 / 状態：**本番デプロイ済み・準備中オーバーレイ付き（`?preview=1` で閲覧可）**

## 概要
プロフィールカード作成ページ。背景画像に文字を重ねるCanvas描画方式。
- URL：https://satoyu.info/card.html （通常アクセスは「準備中」／ https://satoyu.info/card.html?preview=1 で中身が見える）
- 旧カード作成ページは **`card_old.html`** に退避（noindex・同じく準備中オーバーレイ付き）
- テスト用だった `card-test.html` は削除（2026-07-15 に card.html へ昇格）
- 背景画像：**`images/satoyu_osi_profile3.png`（724 × 1022 px）** ← 座標はすべてこの画像基準
- 描画関数：`drawOshiProfile(ctx, w, getV, ico, measureOnly)`（`DRAW_FNS.oshiprofile`）
- テンプレート：`TEMPLATES = [{ id:"oshiprofile", name:"推しぷろふぃーる", w:OSHI_W, customRender:true }]`
- 旧テンプレート（notebook / profilebook 等）の描画関数はコードに残っているが `TEMPLATES` には載せていない（dead code・整理は任意）

## ローカルプレビューの手順
1. プロジェクト直下で `python -m http.server 8899`
2. http://localhost:8899/card.html?preview=1 を開く（更新は Ctrl+F5）
3. ファイルを直して再読み込みで確認 → 区切りがついたらコミット＆デプロイ

**公開するとき**：`card.html` の `<body>` 直後にある `id="coming-soon-overlay"` の div と、その下の1行スクリプトを削除するだけ。

## 位置合わせ用グリッド
`let OSHI_SHOW_GRID = false;` を `true` にすると 50px 赤／100px 青の座標グリッドが描かれる。
ダウンロード画像には出ない（`buildHighResCanvas` で一時的に false にしている）。位置指示のとき以外は **false のまま**。

## 背景画像を差し替えたときの手順（2026-07-15 に実施）
旧 `satoyu_osi_profile.png`（722×1026）→ 新 `satoyu_osi_profile3.png`（724×1022）で、次のズレを実測して座標を補正した。
- カード内の要素が全体的に **横 −3px**（縦は変化なし）
- じこしょうかい枠：右辺 x626 → **x581** に縮小（上下 281〜420 は同じ）
- 雲：縦位置そのままで **左列 +20px / 中央列 −3px / 右列 −20px** の内寄せ

測り方：ヘッドレスChromeで背景画像をcanvasに描き `getImageData` でピクセル走査（暗い線＝枠線、白いラン＝雲）。
一時HTMLを**プロジェクト直下**に置いて `http://localhost:8899/_probe.html` を `--dump-dom` で読む（同一オリジンでないと `getImageData` が SecurityError になる）。

## 入力フォーム（SHARED_FIELDS の順番）
1. かいた日 年/月/日（`wrote-y` / `wrote-m` / `wrote-d`）
2. なまえ（`fan-name`・必須）
3. X ID（`handle`・任意）
4. 推し（`oshi-name`・**固定「佐藤優羽」・編集不可**：`readonly:true, fixed:"佐藤優羽"`）
5. 他推し（`other-oshi`・「さとうゆ一推しなら空欄」）
6. **じこしょうかい**（旧 card.html と同じ構成）
   - 誕生年（`birth-year`・省略可）／誕生月日（`birth-md`・省略可）／性別（`gender`・🚹🚺無回答）
   - 呼ばれ方（`nickname`）／MBTI（`mbti`・17択）／おひさま歴（`ohisama-history`）／自由に自己紹介（`self-intro`・省略可）
   - → `buildSelfIntroText(getV)` が文章を組み立てる（「私は◯◯生まれの◯で、MBTIは◯。みんなには◯って呼ばれてるよ。おひさまになったのは◯からだよ。」＋自由記述）
7. 雲メモ①〜⑥（`cloud-1`〜`cloud-6`・**改行可 / textarea**）
   - ①好きな日向坂46の楽曲 ②好きな日向坂46のMV ③好きなひなあいの企画
   - ④佐藤優羽さんの呼び方 ⑤佐藤優羽さんを好きになったきっかけ ⑥好きなペア
   - カード上の見出しは `CLOUD_LABELS`、⑤だけ2行表示（`CLOUD_LABEL_LINES`）
8. すきなところ ベスト3（`best-1`〜`3`・**改行可 / textarea**・最大3行）
9. 自由記述こーなー（`free-note`・改行可）
10. イラスト選択（`selectedIllusts`・最大3点）

## 描画座標（drawOshiProfile 内・724×1022 基準）
- **かいた日**（中央・y=58）：年 x=372 / 月 x=507 / 日 x=617、font 19 Yomogi
- **なまえ＋X ID**：2行をブロック扱い。左端 **x=125**、ブロックの縦中心を背景の「なまえ」ラベル（実測 y138〜154）の中心 **y=146** に合わせる。表示域は x125〜380（幅255・自動縮小）。X ID はなまえの下に「（@id）」、色はなまえと同じ `OSHI_INK`
  - 「（」は字面の左に余白があるので `measureText().actualBoundingBoxLeft` でインクの左端をそろえている
- **推し行**（y=210）：「推し」x=57 ／「佐藤優羽」x=122。他推しがあれば「他推し」x=247・値 x=307、無ければ「佐藤優羽一推し！」
- **ゆう罪／無罪スタンプ**：他推しの入力があれば `yuzai.png`、無ければ `muzai.png` を **(400,110)–(470,180)** に −6° 傾けて描画（`opStamp`）
- **アイコン写真**：丸型 cx=580 / cy=197 / r=51（cover・白フチ3px）
- **じこしょうかい**：枠 x37〜581・y281〜420。テキストは x53〜565・中心 y348、18px→10px で自動縮小＋折り返し（`opSelfIntro`）
- **雲6つ**：`CLOUD_SPECS` を参照（見出しは縦中心そろえ・`opCloudLabel` ／ 入力文字は枠内で自動縮小＋上下左右中央・`opCloudText`）
- **すきなところ ベスト3**：背景の数字1/2/3の中心（y=782 / 864.5 / 946）にブロック中心を合わせる。x=95・幅252・最大3行（`opBestText`）。手動改行があるときは「1行なら拡大」処理をスキップ
- **自由記述こーなー**：x415〜647、中心 y867、11px〜26px（`opFreeText`）
- **イラスト3枠**（`OSHI_ILLUST_SLOTS`）：
  - じこしょうかい枠の右（cx650 / cy350 / 108px）
  - 雲①④の外側（cx44 / cy550 / 79px）
  - 雲③⑥の外側（cx680 / cy550 / 79px）
  - 選べるイラストは `PICKER_ILLUSTS`（yuzai / muzai を除いた8種）、最大3点（`MAX_ILLUSTS = 3`）

## 描画ヘルパー
- `opLineText`：1行・幅で自動縮小・収まらなければ末尾…
- `opSelfIntro`：じこしょうかい（自動サイズ＋折り返し＋上下中央・左そろえ）
- `opCloudLabel`：雲の見出し（複数行可・行ブロックの縦中心を `ly` に合わせる）
- `opCloudText`：雲の中身（自動縮小＋上下左右中央）
- `opBestText`：ベスト3（1行なら拡大・複数行なら縮小＋中心そろえ）
- `opFreeText`：自由記述（自動サイズ＋中央）
- `opStamp`：ゆう罪／無罪（矩形に収めて傾けて描画・白い台紙なし）
- `opPhoto`：丸型アイコン／`opGrid`：位置指示用グリッド
- `wrapText` は `"\n"` を行区切りとして扱う（＝手動改行が効く）
- 色：`OSHI_INK = "#4a3a2c"`（手書き文字）／`OSHI_INK2 = "#3a4a40"`（雲の中）／雲の見出し `#2f7a54`

## 入力の保存（下書き）
- `localStorage`：`satoyu_card_draft_v1`（値）／`satoyu_card_icon_v1`（アイコン・640pxのJPEGに縮小）
- キーはテンプレートID接頭辞つき（例：`oshiprofile__fan-name`）
- 同一ブラウザ内のみ保持

## 「委員会に共有する」→ Notion（DB_CARDS）
`card.html` の `submitCard()` → `POST /admin-api/cards` → `admin-api/server.js` が Notion に登録。

| カードの項目 | 送信キー | Notion プロパティ |
|---|---|---|
| なまえ | fanName | Name（タイトル） |
| X ID | handle | Handle |
| かいた日 | wroteDate | **WroteDate**（新規） |
| 他推し | otherOshi | OtherOshi |
| 誕生年／誕生月日／性別／MBTI／呼ばれ方／おひさま歴／自由に自己紹介 | birthYear / birthMD / gender / mbti / nickname / ohishamaHistory / selfIntro | BirthYear / BirthMD / Gender / MBTI / Nickname / OhisamaHistory / SelfIntro |
| 雲① 好きな楽曲 | song | Song |
| 雲② 好きなMV | favMV | **FavMV**（新規） |
| 雲③ 好きなひなあいの企画 | favHinaai | **FavHinaai**（新規） |
| 雲④ 呼び方 | yuCallName | **YuCallName**（新規） |
| 雲⑤ 好きになったきっかけ | kikkake | **Kikkake**（新規） |
| 雲⑥ 好きなペア | favPair | **FavPair**（新規） |
| すきなところ ベスト3 | best1 / best2 / best3 | **Best1 / Best2 / Best3**（新規） |
| 自由記述こーなー | freeNote | **FreeNote**（新規） |
| 選んだイラスト | illusts | **Illusts**（新規） |
| カード画像 | imageBase64 | CardImage（URL） |

- 新規追加した11プロパティはすべて **テキスト型**（2026-07-15 に Notion 側で追加済み）
- 旧カードのプロパティ（`BestLive1〜3` / `BestVar1〜3` / `OshiName` / `OshiReason` / `OshiLike` / `OshiLikeFree` / `OshiLove`）は過去レコードのため残す。新カードからは書き込まれない
- 同じ X ID の既存レコードは登録時にアーカイブされる（最新のみ保持）

## カード画像の保存先（VPS）
- 保存先：`/var/www/satoyu/uploads/cards/<uuid>.png`
- 閲覧：`https://satoyu.info/uploads/cards/<uuid>.png`（**Basic認証・ユーザー名 `satoyu`**）
- GitHub Actions の rsync は `--exclude='/uploads/'` なので消えない

## VPS 側で必要だった設定（2026-07-15・リポジトリ管理外 `~/newsmind/`）
**コンテナや設定を作り直すときは、この2点が入っているか必ず確認する。**

1. **nginx のボディサイズ上限**（`~/newsmind/nginx/nginx.conf`）
   satoyu.info のブロックの `location /admin-api/` に `client_max_body_size 20m;` を追加。
   無いと既定1MBのため、新カードの2倍解像度PNG（base64が1MB超）が **413のHTMLページ**で返り、
   フロントで `Unexpected token '<', "<html> <h"... is not valid JSON` になる。
   （Express 側は `express.json({ limit: "5mb" })`）
   ```bash
   cd ~/newsmind
   docker compose exec nginx nginx -t
   docker compose up -d --force-recreate nginx
   ```

2. **uploads のボリュームマウント**（`~/newsmind/docker-compose.yml`）
   `satoyu-admin` の volumes に `- /var/www/satoyu/uploads:/var/www/satoyu/uploads` を追加。
   無いと画像がコンテナ内部にしか書かれず、nginx が配信するホスト側には無いので **404**（コンテナ再作成で消える）。
   ```bash
   cd ~/newsmind
   docker compose up -d --force-recreate satoyu-admin
   ```

`admin-api/server.js` を変更したときは、デプロイ（rsync）後に `docker compose restart satoyu-admin`。
環境変数を足したときは `restart` では読まれないので `up -d --force-recreate satoyu-admin`。

## 今後の候補
- 公開（準備中オーバーレイの削除）
- ダウンロード画像を高精細にするなら、背景画像の2倍解像度版を用意して差し替え
- `card_old.html` は落ち着いたら削除してよい

## 関連ファイル
- `card.html`：本体（フォーム＋描画）
- `card_old.html`：旧カード作成ページ（退避・noindex）
- `images/satoyu_osi_profile3.png`：背景画像
- `images/image/card/*.png`：イラスト素材（yuzai / muzai はスタンプ専用）
- `admin-api/server.js`：`POST /cards`（Notion登録・画像保存）
